// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.

import { isJqError, maybeFilter } from 'cloudflare-mcp/filtering';
import { Metadata, asErrorResult, asTextContentResult } from 'cloudflare-mcp/tools/types';

import { Tool } from '@modelcontextprotocol/sdk/types.js';
import Cloudflare from 'cloudflare';

export const metadata: Metadata = {
  resource: 'realtime_kit.meetings',
  operation: 'write',
  tags: [],
  httpMethod: 'post',
  httpPath: '/accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/participants',
  operationId: 'add_participant',
};

export const tool: Tool = {
  name: 'add_participant_realtime_kit_meetings',
  description:
    "When using this tool, always use the `jq_filter` parameter to reduce the response size and improve performance.\n\nOnly omit if you're sure you don't need the data.\n\nAdds a participant to the given meeting ID.\n\n# Response Schema\n```json\n{\n  $ref: '#/$defs/meeting_add_participant_response',\n  $defs: {\n    meeting_add_participant_response: {\n      type: 'object',\n      title: 'GenericSuccessResponse',\n      properties: {\n        success: {\n          type: 'boolean',\n          description: 'Success status of the operation'\n        },\n        data: {\n          type: 'object',\n          title: 'Participant',\n          description: 'Represents a participant.',\n          properties: {\n            id: {\n              type: 'string',\n              description: 'ID of the participant.'\n            },\n            token: {\n              type: 'string',\n              description: 'The participant\\'s auth token that can be used for joining a meeting from the client side.'\n            },\n            created_at: {\n              type: 'string',\n              description: 'When this object was created. The time is returned in ISO format.',\n              format: 'date-time'\n            },\n            custom_participant_id: {\n              type: 'string',\n              description: 'A unique participant ID generated by the client.'\n            },\n            preset_name: {\n              type: 'string',\n              description: 'Preset applied to the participant.'\n            },\n            updated_at: {\n              type: 'string',\n              description: 'When this object was updated. The time is returned in ISO format.',\n              format: 'date-time'\n            },\n            name: {\n              type: 'string',\n              description: 'Name of the participant.'\n            },\n            picture: {\n              type: 'string',\n              description: 'URL to a picture of the participant.'\n            }\n          },\n          required: [            'id',\n            'token',\n            'created_at',\n            'custom_participant_id',\n            'preset_name',\n            'updated_at'\n          ]\n        }\n      },\n      required: [        'success'\n      ]\n    }\n  }\n}\n```",
  inputSchema: {
    type: 'object',
    properties: {
      account_id: {
        type: 'string',
        description: 'The account identifier tag.',
      },
      app_id: {
        type: 'string',
        description: 'The app identifier tag.',
      },
      meeting_id: {
        type: 'string',
      },
      custom_participant_id: {
        type: 'string',
        description:
          'A unique participant ID. You must specify a unique ID for the participant, for example, UUID, email address, and so on. ',
      },
      preset_name: {
        type: 'string',
        description: 'Name of the preset to apply to this participant.',
      },
      name: {
        type: 'string',
        description: '(Optional) Name of the participant. ',
      },
      picture: {
        type: 'string',
        description: '(Optional) A URL to a picture to be used for the participant. ',
      },
      jq_filter: {
        type: 'string',
        title: 'jq Filter',
        description:
          'A jq filter to apply to the response to include certain fields. Consult the output schema in the tool description to see the fields that are available.\n\nFor example: to include only the `name` field in every object of a results array, you can provide ".results[].name".\n\nFor more information, see the [jq documentation](https://jqlang.org/manual/).',
      },
    },
    required: ['account_id', 'app_id', 'meeting_id', 'custom_participant_id', 'preset_name'],
  },
  annotations: {},
};

export const handler = async (client: Cloudflare, args: Record<string, unknown> | undefined) => {
  const { meeting_id, jq_filter, ...body } = args as any;
  try {
    return asTextContentResult(
      await maybeFilter(jq_filter, await client.realtimeKit.meetings.addParticipant(meeting_id, body)),
    );
  } catch (error) {
    if (error instanceof Cloudflare.APIError || isJqError(error)) {
      return asErrorResult(error.message);
    }
    throw error;
  }
};

export default { metadata, tool, handler };
